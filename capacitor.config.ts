import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'space.riinswork.missmarica',
  appName: 'MissMarica',
  webDir: '.output/public',
  server: {
    androidScheme: 'https'
  }
};

export default config;
